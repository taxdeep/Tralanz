namespace Citus.Ui.Shared.Icons;

/// <summary>
/// Three fixed icon sizes (UI_NAVIGATION_AND_DESIGN_SYSTEM_SPEC §11).
/// Use <see cref="Inline"/> next to text, <see cref="Action"/> on buttons
/// and menu items, <see cref="Display"/> on page headers and empty states.
/// </summary>
public enum IconSize
{
    Inline = 0,
    Action = 1,
    Display = 2
}
