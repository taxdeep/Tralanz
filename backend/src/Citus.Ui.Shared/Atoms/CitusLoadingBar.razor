@*
    CitusLoadingBar — indeterminate horizontal loading line. Phase 6
    of the UI library migration: provides the single Citus/Radzen
    "busy, no number" loading shape used across the app.

    Wraps Radzen.RadzenProgressBar in indeterminate mode. The Class
    parameter passes through for page-level spacing.
*@

@using Radzen
@using Radzen.Blazor

<RadzenProgressBar Mode="ProgressBarMode.Indeterminate"
                   ShowValue="false"
                   Class="@(string.IsNullOrEmpty(Class) ? "citus-loading-bar" : $"citus-loading-bar {Class}")" />

@code {
    [Parameter] public string? Class { get; set; }
}
