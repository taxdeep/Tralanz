@inject WebShellState ShellState
@inject Web.Shell.Services.WebShellBusinessSessionClient SessionClient
@inject Web.Shell.Services.WebShellCompanyOnboardingClient OnboardingClient
@inject Web.Shell.Services.WebShellSessionExpirationCoordinator SessionExpirationCoordinator

<nav class="ws-nav">
    <div class="ws-nav__section">
        <div class="ws-nav__section-title">Workspace</div>
        <NavLink href="/" Match="NavLinkMatch.All" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.Dashboard" Size="Size.Small" />
            <span class="ws-nav__label">Dashboard</span>
        </NavLink>
        <NavLink href="/documents/source-browser" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.Article" Size="Size.Small" />
            <span class="ws-nav__label">Source Documents</span>
        </NavLink>
        <NavLink href="/profile" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.Person" Size="Size.Small" />
            <span class="ws-nav__label">Profile</span>
        </NavLink>
    </div>

    <div class="ws-nav__section">
        <div class="ws-nav__section-title">Receivables</div>
        <NavLink href="/ar/receive-payment" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.Payments" Size="Size.Small" />
            <span class="ws-nav__label">Receive Payment</span>
        </NavLink>
        <NavLink href="/ar/credit-application" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.AssignmentReturn" Size="Size.Small" />
            <span class="ws-nav__label">Credit Application</span>
        </NavLink>
        <NavLink href="/documents/source-browser?sourceType=quote" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.RequestQuote" Size="Size.Small" />
            <span class="ws-nav__label">Quotes</span>
        </NavLink>
        <NavLink href="/documents/source-browser?sourceType=sales_order" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.AssignmentTurnedIn" Size="Size.Small" />
            <span class="ws-nav__label">Sales Orders</span>
        </NavLink>
    @if (WritesBlocked)
    {
        <span class="ws-nav__link ws-nav__link--disabled" title="@ShellState.WriteBlockMessage">
            <MudIcon Icon="@Icons.Material.Filled.RequestQuote" Size="Size.Small" />
            <span class="ws-nav__label">Quote Draft</span>
        </span>
        <span class="ws-nav__link ws-nav__link--disabled" title="@ShellState.WriteBlockMessage">
            <MudIcon Icon="@Icons.Material.Filled.AssignmentTurnedIn" Size="Size.Small" />
            <span class="ws-nav__label">SO Draft</span>
        </span>
        <span class="ws-nav__link ws-nav__link--disabled" title="@ShellState.WriteBlockMessage">
            <MudIcon Icon="@Icons.Material.Filled.ReceiptLong" Size="Size.Small" />
            <span class="ws-nav__label">Invoice Draft</span>
        </span>
        <span class="ws-nav__link ws-nav__link--disabled" title="@ShellState.WriteBlockMessage">
            <MudIcon Icon="@Icons.Material.Filled.CreditScore" Size="Size.Small" />
            <span class="ws-nav__label">Credit Note Draft</span>
        </span>
    }
    else
    {
        <NavLink href="/documents/quote/draft-editor" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.RequestQuote" Size="Size.Small" />
            <span class="ws-nav__label">Quote Draft</span>
        </NavLink>
        <NavLink href="/documents/sales-order/draft-editor" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.AssignmentTurnedIn" Size="Size.Small" />
            <span class="ws-nav__label">SO Draft</span>
        </NavLink>
        <NavLink href="/documents/invoice/draft-editor" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.ReceiptLong" Size="Size.Small" />
            <span class="ws-nav__label">Invoice Draft</span>
        </NavLink>
        <NavLink href="/documents/credit-note/draft-editor" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.CreditScore" Size="Size.Small" />
            <span class="ws-nav__label">Credit Note Draft</span>
        </NavLink>
    }
    </div>

    <div class="ws-nav__section">
        <div class="ws-nav__section-title">Payables</div>
        <NavLink href="/ap/pay-bill" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.RequestQuote" Size="Size.Small" />
            <span class="ws-nav__label">Pay Bill</span>
        </NavLink>
        <NavLink href="/documents/source-browser?sourceType=purchase_order" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.Assignment" Size="Size.Small" />
            <span class="ws-nav__label">Purchase Orders</span>
        </NavLink>
        <NavLink href="/documents/source-browser?sourceType=receipt" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.Inventory" Size="Size.Small" />
            <span class="ws-nav__label">Receipts</span>
        </NavLink>
        <NavLink href="/documents/purchase-orders/approval-requests" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.Approval" Size="Size.Small" />
            <span class="ws-nav__label">PO Approvals</span>
        </NavLink>
    @if (WritesBlocked)
    {
        <span class="ws-nav__link ws-nav__link--disabled" title="@ShellState.WriteBlockMessage">
            <MudIcon Icon="@Icons.Material.Filled.AddBusiness" Size="Size.Small" />
            <span class="ws-nav__label">PO Draft</span>
        </span>
    }
    else
    {
        <NavLink href="/documents/purchase-order/draft-editor" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.AddBusiness" Size="Size.Small" />
            <span class="ws-nav__label">PO Draft</span>
        </NavLink>
    }
    @if (WritesBlocked)
    {
        <span class="ws-nav__link ws-nav__link--disabled" title="@ShellState.WriteBlockMessage">
            <MudIcon Icon="@Icons.Material.Filled.MoveToInbox" Size="Size.Small" />
            <span class="ws-nav__label">Receipt Draft</span>
        </span>
    }
    else
    {
        <NavLink href="/documents/receipt/draft-editor" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.MoveToInbox" Size="Size.Small" />
            <span class="ws-nav__label">Receipt Draft</span>
        </NavLink>
    }
        <NavLink href="/ap/vendor-credit-application" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.AssignmentTurnedIn" Size="Size.Small" />
            <span class="ws-nav__label">Vendor Credit</span>
        </NavLink>
    @if (WritesBlocked)
    {
        <span class="ws-nav__link ws-nav__link--disabled" title="@ShellState.WriteBlockMessage">
            <MudIcon Icon="@Icons.Material.Filled.Description" Size="Size.Small" />
            <span class="ws-nav__label">Bill Draft</span>
        </span>
    }
    else
    {
        <NavLink href="/documents/bill/draft-editor" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.Description" Size="Size.Small" />
            <span class="ws-nav__label">Bill Draft</span>
        </NavLink>
    }
    @if (WritesBlocked)
    {
        <span class="ws-nav__link ws-nav__link--disabled" title="@ShellState.WriteBlockMessage">
            <MudIcon Icon="@Icons.Material.Filled.Article" Size="Size.Small" />
            <span class="ws-nav__label">Vendor Credit Draft</span>
        </span>
    }
    else
    {
        <NavLink href="/documents/vendor-credit/draft-editor" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.Article" Size="Size.Small" />
            <span class="ws-nav__label">Vendor Credit Draft</span>
        </NavLink>
    }
    </div>

    <div class="ws-nav__section">
        <div class="ws-nav__section-title">Company</div>
        <NavLink href="/company/book-governance" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.MenuBook" Size="Size.Small" />
            <span class="ws-nav__label">Book Governance</span>
        </NavLink>
        <NavLink href="/company/bank-and-cash-setup" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.AccountBalance" Size="Size.Small" />
            <span class="ws-nav__label">Bank & Cash</span>
        </NavLink>
        <NavLink href="/company/tax-setup" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.Percent" Size="Size.Small" />
            <span class="ws-nav__label">Tax Setup</span>
        </NavLink>
        <NavLink href="/company/customer-vendor-setup" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.Handshake" Size="Size.Small" />
            <span class="ws-nav__label">Customers & Vendors</span>
        </NavLink>
        <NavLink href="/company/product-service-setup" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.Inventory2" Size="Size.Small" />
            <span class="ws-nav__label">Products & Services</span>
        </NavLink>
    </div>

    @if (_showInventoryFoundation)
    {
        <div class="ws-nav__section">
            <div class="ws-nav__section-title">Inventory</div>
            <NavLink href="/company/inventory-foundation" class="ws-nav__link">
                <MudIcon Icon="@Icons.Material.Filled.Warehouse" Size="Size.Small" />
                <span class="ws-nav__label">Foundation</span>
            </NavLink>
            <NavLink href="/company/inventory-purchase-receipt" class="ws-nav__link">
                <MudIcon Icon="@Icons.Material.Filled.MoveToInbox" Size="Size.Small" />
                <span class="ws-nav__label">Receipt Control</span>
            </NavLink>
            <NavLink href="/company/inventory-shipment" class="ws-nav__link">
                <MudIcon Icon="@Icons.Material.Filled.LocalShipping" Size="Size.Small" />
                <span class="ws-nav__label">Shipment</span>
            </NavLink>
            <NavLink href="/company/inventory-transfer" class="ws-nav__link">
                <MudIcon Icon="@Icons.Material.Filled.SyncAlt" Size="Size.Small" />
                <span class="ws-nav__label">Warehouse Transfer</span>
            </NavLink>
            <NavLink href="/company/inventory-availability" class="ws-nav__link">
                <MudIcon Icon="@Icons.Material.Filled.FactCheck" Size="Size.Small" />
                <span class="ws-nav__label">Availability</span>
            </NavLink>
        </div>
    }

    <div class="ws-nav__section">
        <div class="ws-nav__section-title">Governance</div>
        <NavLink href="/company/membership-permissions" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.Groups" Size="Size.Small" />
            <span class="ws-nav__label">Memberships</span>
        </NavLink>
        <NavLink href="/company/open-item-adjustment-account-mappings" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.Schema" Size="Size.Small" />
            <span class="ws-nav__label">Adjustment Mapping</span>
        </NavLink>
    @if (WritesBlocked)
    {
        <span class="ws-nav__link ws-nav__link--disabled" title="@ShellState.WriteBlockMessage">
            <MudIcon Icon="@Icons.Material.Filled.EditNote" Size="Size.Small" />
            <span class="ws-nav__label">Journal Entry</span>
        </span>
    }
    else
    {
        <NavLink href="/gl/journal-entries" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.EditNote" Size="Size.Small" />
            <span class="ws-nav__label">Journal Entry</span>
        </NavLink>
    }
        <NavLink href="/settings/user-preferences/system-setup" class="ws-nav__link">
            <MudIcon Icon="@Icons.Material.Filled.Tune" Size="Size.Small" />
            <span class="ws-nav__label">Preferences</span>
        </NavLink>
    </div>
</nav>

@code {
    private bool _showInventoryFoundation = true;

    private bool WritesBlocked => ShellState.AreWritesBlocked;

    protected override async Task OnInitializedAsync()
    {
        await ShellState.EnsureHydratedAsync();
        var probe = await SessionClient.ProbeContextAsync();
        if (await SessionExpirationCoordinator.HandleProbeAsync(probe))
        {
            return;
        }

        var onboardingResult = await OnboardingClient.GetAsync();
        if (!onboardingResult.RequiresSignIn &&
            !onboardingResult.IsNotFound &&
            string.IsNullOrWhiteSpace(onboardingResult.ErrorMessage) &&
            onboardingResult.Value is not null)
        {
            _showInventoryFoundation = onboardingResult.Value.SupportsInventoryFoundation;
        }
    }
}
